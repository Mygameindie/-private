// core.js
(function () {
  // global namespace (optional)
  window.APP = window.APP || {};
  // Track currently selected item for color changing (kept for future features)
  window.currentlySelectedItem = null;
})();
